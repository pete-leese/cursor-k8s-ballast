global:
  scrape_interval: 5s
  evaluation_interval: 5s
rule_files:
  - /etc/prometheus/rules.yml
scrape_configs:
  - job_name: kube-state-metrics-stub
    metrics_path: /metrics
    static_configs:
      - targets: ["ballast-ksm:8081"]
