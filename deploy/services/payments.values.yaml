# payments — the upstream service every other service depends on (topology.yaml).
# This is the service the demo's bad chart bump targets.
fullnameOverride: payments
replicaCount: 2
ballastMb: 40
service:
  version: "0.1.0"
resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 16Mi
