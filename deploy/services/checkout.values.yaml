# checkout — depends on payments (topology.yaml).
fullnameOverride: checkout
replicaCount: 1
ballastMb: 40
service:
  version: "0.1.0"
resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi
