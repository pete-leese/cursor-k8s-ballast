# ballast RCA engine dependencies.
pydantic>=2.6
PyYAML>=6.0
httpx>=0.27
# MCP server (read-only triage/RCA tools exposed to a Cursor agent).
mcp>=1.2
# Ballast console (FastAPI + Streamlit).
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.32
requests>=2.31
# Evidence screenshots (ArgoCD / Prometheus / Grafana via headless Chromium).
playwright>=1.40
